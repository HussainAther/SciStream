"""Reserved boundary for future real-time room transport.

The earlier prototype imported Django Channels without declaring or configuring
it. The focused hackathon workflow intentionally uses local chat state, so this
module remains dependency-free until authenticated room semantics are designed.
"""
